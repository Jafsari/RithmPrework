Nested Objects Exercises

Part 1
1. var arr = nestedData.numberData.primeNumbers
for(var i = 0; i < arr.length ; i ++) {
	console.log(arr[i])
}
2. var arr = nestedData.numberData.fibonnaci
for(var i = 0; i < arr.length ; i ++) {
	if (arr[i] %. 2 === 0) {
	console.log(arr[i])
	}
}

3. var arr = nestedData.innerData.order
console.log(arr[1])

4. addSnack('chocolate');

5. 'this' refers to the parameter being passed into.

Part II

1. function addSpeaker(speakerName){
  nestedObject.speakers.push({
    name: speakerName
  })
}

2. function addLanguage (language,helloFirst) {
nestedObject.data.languages[farsi] = {
	hello: helloFirst
}
}

3. function addCountry(name, capital, population){
  nestedObject.data.continents.europe.countries[name] = {
    capital: capital,
    population: population
  }
}